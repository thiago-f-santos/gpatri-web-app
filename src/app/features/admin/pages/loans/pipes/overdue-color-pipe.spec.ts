import { OverdueColorPipe } from './overdue-color-pipe';

describe('OverdueColorPipe', () => {
  it('create an instance', () => {
    const pipe = new OverdueColorPipe();
    expect(pipe).toBeTruthy();
  });
});
